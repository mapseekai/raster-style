# 开发约定

协议变更需同步更新 `spec/v2/`、共享测试和相关文档。依次运行 `pnpm generate`、三种语言的格式化工具和 `pnpm test:all`。

核心字段描述像元渲染和图像输出；图例、类别标签、自动分类与版面由上层应用管理。查询参数使用有序键值对或多值映射，保留重复键和顺序。未知字段返回错误，Q2 与数据源、鉴权参数分别校验。

业务代码使用语义清晰的命名、小函数和可读源码。生成资源限于 Schema、绑定表和类型，并注明来源与生成命令。

提交前运行 `git diff --check`。发布前确认各语言包附带 MIT 许可证，并核对远程模块路径及各注册表的包所有权。
