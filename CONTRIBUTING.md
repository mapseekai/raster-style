# 开发约定

任何协议变更必须同时更新 `spec/v2/`、共享测试及相关文档。先运行 `pnpm generate`，再运行三种语言的格式化工具，最后执行 `pnpm test:all`。

禁止新增图例、类别标签、自动分类向导或版面字段。禁止用普通单值 map 代替重复查询参数。禁止静默丢弃未知字段，或将输出 QUERY 与外层数据源/鉴权参数混合校验。

业务代码使用清晰语义命名和小函数；不提交压缩源码。只有 Schema/绑定/类型资源允许生成，生成来源与命令必须明确。

提交前检查 `git diff --check`。发布前需要明确许可证、远程模块路径以及各注册表的包所有权；当前初始化不执行发布。
